# 股票系统工作规则

## 时间确认规则

在执行任何股票、行情、交易日、回测、信号、数据库查询、数据同步、报表生成相关任务之前，必须先确认当前真实时间。

不要只依赖模型上下文里的日期。必须优先通过系统命令确认当前时间，例如：

```bash
date '+%Y-%m-%d %H:%M:%S %Z %z'

1. 操作本地mysql数据库时，禁止DROP、TRUNCATE删除数据表；
2. 禁止执行rm、批量删除本地代码文件；
3. 暂时忽略ml_predict文件夹里的代码，因为ml_predict文件夹里的代码是机器学习模型训练代码，不是股票系统里的代码。
